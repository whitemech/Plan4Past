(define (domain blocks-domain)
    (:requirements :conditional-effects :derived-predicates :negative-preconditions :strips)
    (:types block)
    (:predicates (clear ?x) (emptyhand) (holding ?x) (on ?x ?y) (ontable ?x) (val_on_a_g) (val_on_a_g-and-on_g_d-and-on_d_b-and-on_b_c-and-on_c_f-and-on_f_e) (val_on_b_c) (val_on_c_f) (val_on_d_b) (val_on_f_e) (val_on_g_d))
    (:derived (val_on_a_g) (on a g))
     (:derived (val_on_a_g-and-on_g_d-and-on_d_b-and-on_b_c-and-on_c_f-and-on_f_e) (and (val_on_a_g) (val_on_g_d) (val_on_d_b) (val_on_b_c) (val_on_c_f) (val_on_f_e)))
     (:derived (val_on_b_c) (on b c))
     (:derived (val_on_c_f) (on c f))
     (:derived (val_on_d_b) (on d b))
     (:derived (val_on_f_e) (on f e))
     (:derived (val_on_g_d) (on g d))
    (:action pick-up
        :parameters (?x)
        :precondition (and (clear ?x) (ontable ?x) (emptyhand))
        :effect (and (and (not (ontable ?x)) (not (clear ?x)) (not (emptyhand)) (holding ?x)))
    )
     (:action put-down
        :parameters (?x)
        :precondition (holding ?x)
        :effect (and (and (not (holding ?x)) (clear ?x) (emptyhand) (ontable ?x)))
    )
     (:action stack
        :parameters (?x ?y)
        :precondition (and (holding ?x) (clear ?y))
        :effect (and (and (not (holding ?x)) (not (clear ?y)) (clear ?x) (emptyhand) (on ?x ?y)))
    )
     (:action unstack
        :parameters (?x ?y)
        :precondition (and (on ?x ?y) (clear ?x) (emptyhand))
        :effect (and (and (holding ?x) (clear ?y) (not (clear ?x)) (not (emptyhand)) (not (on ?x ?y))))
    )
)